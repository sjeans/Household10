window.SelectReset = (elementId, newVal) => {
    let e = document.getElementById(elementId);
    e.value = newVal;
}
function ResetSelectValue(id, newVal) {
    let e = document.getElementById(id);
    e.value = newVal;
}