window.ResetSelector = (id, newVal) => {
    let e = document.getElementById(id);
    e.value = newVal;
}

function ResetSelectValue(id, newVal) {
    let e = document.getElementById(id);
    e.value = newVal;
}
